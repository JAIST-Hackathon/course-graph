# ──────────── Frontend (Node/Vite) ────────────
node_modules/           # 依存パッケージ
npm-debug.log*          # npm エラー
yarn-error.log*         # Yarn エラー
pnpm-debug.log*         # pnpm エラー
dist/                   # ビルド出力
build/                  # 他ビルドツール出力
.temp/                  # 一時ディレクトリ
.cache/                 # キャッシュ
.vite/                  # Vite キャッシュ

# ──────────── Backend (Python/FastAPI) ────────────
.venv/                  # 仮想環境（venv）
venv/                   # 仮想環境（別名）
__pycache__/            # Python バイトコード
*.py[cod]               # コンパイル済み Python ファイル
.coverage               # カバレッジレポート
htmlcov/                # カバレッジ HTML 出力

# ──────────── 環境変数 / 秘密情報 ────────────
.env                    # dotenv 実ファイル
.env.*                  # .env.local など

# ──────────── ログ・一時ファイル ────────────
logs/                   # ログディレクトリ
*.log                   # ログファイル
*.log.*                 # ログローテートファイル
nohup.out               # バックグラウンド実行ログ

# ──────────── エディタ・IDE ────────────
.vscode/                # VSCode ワークスペース
!.vscode/extensions.json# 拡張推奨リストはコミット
.idea/                  # IntelliJ 系設定
*.suo                   # Visual Studio
*.ntvs*                 # .NET 関連
*.njsproj               # Node.js Tools for VS
*.sln                   # Visual Studio ソリューション
*.sw?                   # Vim swap

# ──────────── OS 固有ファイル ────────────
.DS_Store               # macOS Finder
Thumbs.db               # Windows Thumbnails
desktop.ini             # Windows フォルダ設定
._*                     # macOS Spotlight（リソースフォーク）

# ──────────── CI/テスト成果物 ────────────
/coverage/              # テストカバレッジ
/reports/               # テストレポート

# ──────────── ドキュメントビルド ────────────
/docs/_build/           # Sphinx, MkDocs など
/site/                  # 静的サイト生成出力

# ──────────── 追記用エリア ────────────
# もし他に無視したいファイルやフォルダがあれば、この下に追加してください。

# 不要なフレームワーク特有ファイル（.next、.nuxt、bower_components など）は外しています。
# lefthook.toml や .github/ 配下は設定ファイルなので、基本的にコミットしておきます。
# SCode の拡張リストだけは共有したいので、.vscode/extensions.json はコミット対象にしています。